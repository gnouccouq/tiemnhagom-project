{
  "name": "tiemnhagom-loyalty-app",
  "version": "1.0.0",
  "scripts": {
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap add android && npx cap open android",
    "cap:ios": "npx cap add ios && npx cap open ios",
    "cap:build:android": "npx cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0",
    "firebase": "^12.12.1",
    "iconv-lite": "^0.7.3"
  }
}

